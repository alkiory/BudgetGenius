{
  "name": "mobile",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "author": "Alkiory",
  "scripts": {
    "build": "VITE_CAPACITOR=true pnpm --filter frontend-web build && npx cap sync",
    "build:android": "npx cap sync && npx cap open android",
    "sync": "npx cap sync",
    "dev:android": "CAP_DEV=true npx cap sync"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^7.5.0",
    "@capacitor/core": "^7.6.7",
    "@capacitor/status-bar": "^7.0.6"
  },
  "devDependencies": {
    "@capacitor/android": "^7.6.7",
    "@capacitor/cli": "^7.6.7",
    "typescript": "~5.7.3"
  }
}
